// Funciones relacionadas con la interfaz de usuario de Quarum

function toggleMenu() {
    const menu = document.querySelector(".menu");
    if (menu) {
        menu.classList.toggle("active");
    }
}
