<svg width="100%" height="240" viewBox="0 0 1200 240" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <!-- Fondo degradado premium -->
    <linearGradient id="bgGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#000814"/>
      <stop offset="100%" stop-color="#001d3d"/>
    </linearGradient>

    <!-- Gradiente neón -->
    <linearGradient id="qGrad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#00E1FF"/>
      <stop offset="100%" stop-color="#007BFF"/>
    </linearGradient>

    <!-- Brillo neón -->
    <filter id="neonGlow">
      <feGaussianBlur stdDeviation="5" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- Líneas animadas premium -->
    <linearGradient id="lineGrad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#00E1FF" stop-opacity="0"/>
      <stop offset="50%" stop-color="#00E1FF" stop-opacity="1"/>
      <stop offset="100%" stop-color="#00E1FF" stop-opacity="0"/>
    </linearGradient>
  </defs>

  <!-- Fondo -->
  <rect width="1200" height="240" fill="url(#bgGrad)"/>

  <!-- Líneas animadas premium -->
  <g opacity="0.35">
    <rect x="-400" y="60" width="500" height="3" fill="url(#lineGrad)">
      <animate attributeName="x" from="-400" to="1200" dur="5s" repeatCount="indefinite"/>
    </rect>

    <rect x="-600" y="120" width="700" height="3" fill="url(#lineGrad)">
      <animate attributeName="x" from="-600" to="1200" dur="7s" repeatCount="indefinite"/>
    </rect>

    <rect x="-500" y="180" width="600" height="3" fill="url(#lineGrad)">
      <animate attributeName="x" from="-500" to="1200" dur="6s" repeatCount="indefinite"/>
    </rect>
  </g>

  <!-- Marco del logo premium -->
  <rect x="80" y="60" width="110" height="110" rx="22"
        fill="none" stroke="url(#qGrad)" stroke-width="5"
        filter="url(#neonGlow)">
    <animate attributeName="stroke-width" values="4;5;4" dur="3s" repeatCount="indefinite"/>
  </rect>

  <!-- Símbolo Q premium -->
  <path d="M135 85
           A30 30 0 1 1 134.9 85
           M155 135 L178 158"
        stroke="url(#qGrad)" stroke-width="6" fill="none"
        stroke-linecap="round" filter="url(#neonGlow)">
    <animate attributeName="stroke-width" values="5;6;5" dur="2.5s" repeatCount="indefinite"/>
  </path>

  <!-- Texto QUARUM premium -->
  <text x="240" y="120" font-family="Segoe UI, sans-serif" font-size="60"
        fill="white" font-weight="700" letter-spacing="2">
    Quarum
    <animate attributeName="opacity" values="0.8;1;0.8" dur="3s" repeatCount="indefinite"/>
  </text>

  <!-- Subtítulo premium -->
  <text x="240" y="160" font-family="Segoe UI, sans-serif" font-size="26"
        fill="#00E1FF" opacity="0.85">
    Plataforma tecnológica en desarrollo
  </text>
</svg>
